;(function () {
    'use strict';

    describe('Module: todo (app.js)', function () {
        it('should be defined and accesible through angular module system.', function () {
            expect(angular.module('todo')).toBeDefined();
        });
    });
})();
