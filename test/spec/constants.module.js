;(function () {
    'use strict';

    describe('Module: todo.constants (constants.module.js)', function () {
        it('should be defined and accesible through angular module system.', function () {
            expect(angular.module('todo.constants')).toBeDefined();
        });
    });
})();
